@using Models
@using Servicos
@model Models.Equipe

@{
    ViewData["Title"] = "Edit";
}

<h1>Editar</h1>

<h4>Equipe</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Codigo" class="control-label"></label>
                <input asp-for="Codigo" class="form-control" />
                <span asp-validation-for="Codigo" class="text-danger"></span>
            </div>


            <div class="form-group">
                <label asp-for="Pessoa" class="control-label">Selecione as Pessoas da Equipe</label><br/>
                @foreach (var pessoa in await BuscaPessoa.BuscarTodasPessoas())
				{
					<input name="adicionarPessoa" type="checkbox" value=@pessoa.Nome>
					<label for="@pessoa.Nome"> @pessoa.Nome</label><br />
				}

				@*<label asp-for="Pessoa" class="control-label">Pessoas da Equipe: </label><br />
				@foreach (var pessoa in ViewBag.Pessoa)
				{
					<input name="removerPessoa" type="checkbox" value=@pessoa.Id>

					<label for="@pessoa.Nome"> @pessoa.Nome</label><br />
				}
                <input asp-for="Pessoa" class="form-control" />
                <span asp-validation-for="Pessoa" class="text-danger"></span>*@
            </div>


            <div class="form-group">
				</> <label asp-for="Cidade" class="control-label">Selecione a nova cidade</label><br />

				
				<select asp-for="Cidade" id="Cidade">
					@foreach (Cidade cidade in await BuscaCidade.BuscarTodasCidades())
					{
						<option value="@cidade.Nome"> @cidade.Nome </option>
					}
				</select>
			</div>



          @*  <div class="form-group">
                <label asp-for="Cidade" class="control-label"></label>
                <input asp-for="Cidade" class="form-control" />
                <span asp-validation-for="Cidade" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <input type="submit" value="Salvar Alteração" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Cancelar e Voltar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
